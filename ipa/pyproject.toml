[project]
name = "in-pocket-analysis"
version = "0.1.0"
description = "In-Pocket Analysis: A protein-ligand interaction analysis tool."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gemmi>=0.7.3",
    "numpy>=2.3.2",
    "rdkit>=2025.3.5",
]
